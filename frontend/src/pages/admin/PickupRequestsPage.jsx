import PageShell from '../../components/PageShell'

export default function PickupRequestsPage() {
  return (
    <PageShell
      title="Pickup Requests"
      description="Manage resident pickup requests and classification results."
    />
  )
}
